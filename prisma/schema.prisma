generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model artifacts {
  id      String    @id @default(auto()) @map("_id") @db.ObjectId
  images  String[]
  bonus   String[]
  name    String    @unique
  stars   Int
}